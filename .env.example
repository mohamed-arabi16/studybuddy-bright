# StudyBuddy Environment Configuration Template
# Copy this file to .env.local and fill in the values

# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Your Supabase project URL (required)
VITE_SUPABASE_URL="https://YOUR_PROJECT_ID.supabase.co"

# Your Supabase publishable anon key (safe for client-side)
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"

# Your Supabase project ID (for reference)
VITE_SUPABASE_PROJECT_ID="YOUR_PROJECT_ID"

# ============================================
# ERROR TRACKING (Sentry)
# ============================================
# Sentry DSN for error tracking (optional, but recommended for production)
# Leave empty or remove to disable Sentry
VITE_SENTRY_DSN=""

# ============================================
# EDGE FUNCTION SECRETS (set via Supabase CLI or Dashboard)
# These are NOT prefixed with VITE_ as they are server-side only
# ============================================
# SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"  # NEVER expose client-side
# STRIPE_SECRET_KEY="sk_test_..."                     # Stripe API key
# STRIPE_WEBHOOK_SECRET="whsec_..."                   # Stripe webhook signing secret
# LOVABLE_API_KEY="your-lovable-api-key"             # AI API key
# GOOGLE_CLIENT_ID="your-google-client-id"           # OAuth client ID
# GOOGLE_CLIENT_SECRET="your-google-client-secret"   # OAuth client secret
# TOKEN_ENCRYPTION_KEY="your-32-byte-hex-key"        # AES-256 encryption key

# ============================================
# ENVIRONMENT INDICATOR
# ============================================
# This is automatically set by Vite based on build mode
# Values: development, staging, production
# VITE_APP_ENV="development"
